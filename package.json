{
  "name": "ashen-front",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Ashen Front - playable vertical slice (Three.js)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
